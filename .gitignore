# WireGuard keys (NEVER commit)
*.key

# Real configs (contain private keys)
*.conf

# Allow templates explicitly
!*.conf.example

# Service secrets (docker-compose env files)
.env
*.env
!*.env.example

# Backups / temporary
*.bak
*.orig

# Contracts / PDFs
*.pdf
