# Copy to ~/homelab/pihole/.env on the Pi and fill in real values.
# Then restrict it:  chmod 600 .env
#
# This file is git-ignored. Never commit real secrets.

# Password for the Pi-hole admin web interface
PIHOLE_PASSWORD=change_me_to_a_strong_password

# Timezone, used for logs and scheduled tasks
TZ=Europe/Madrid
